import React from 'react'
import ReactDOM from 'react-dom/client'
import Viewport from './components/templates/Viewport'

import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Viewport />
  </React.StrictMode>,
)
